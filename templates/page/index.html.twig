{% extends 'base.html.twig' %}

{% block title %}LibraNova - La plus grande e-bibliothèque de France{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .carousel-container {
            scroll-behavior: smooth;
        }
        .carousel-item {
            transition: all 0.3s ease;
        }
        .carousel-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
        }
    </style>
{% endblock %}

{% block body %}
<!-- Hero Section -->
<section class="relative py-32 bg-cover bg-center" style="background-image: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)), url('https://expodif.fr/wp-content/uploads/2024/10/stack-books-with-library-scene-1024x512.jpg')">
 <div class="container mx-auto px-4 text-center text-white">
     <h1 class="text-5xl font-bold mb-6">Bienvenue sur LibraNova</h1>
     <p class="text-xl mb-10 max-w-3xl mx-auto">Découvrez un monde de littérature, partagez vos passions et explorez de nouveaux horizons.</p>
     <div class="flex justify-center space-x-4">
             <a href="{{ path('app_register') }}" class="px-6 py-3 bg-transparent border-2 border-white text-white hover:bg-white hover:text-blue-800 rounded-lg transition duration-300">Rejoindre la communauté</a>
     </div>
 </div>
</section>

<div class="container mx-auto px-4 py-8">
    <!-- Section Livres Récemment Publiés -->
    <section class="mb-16">
        <div class="flex justify-between items-center mb-4">
            <h2 class="text-2xl font-semibold">Livres Récemment Publiés</h2>
            <div class="flex gap-2">
                <button id="newest-prev" class="bg-gray-200 hover:bg-gray-300 rounded-full p-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                    </svg>
                </button>
                <button id="newest-next" class="bg-gray-200 hover:bg-gray-300 rounded-full p-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                    </svg>
                </button>
            </div>
        </div>

        <div class="relative">
            <div id="newest-carousel" class="carousel-container flex overflow-x-scroll overflow-y-hidden hide-scrollbar gap-4 pb-4">
                {% if booksNewest|length > 0 %}
                    {% for book in booksNewest %}
                        <div class="carousel-item flex-none w-64">
                            <div class="bg-white rounded-lg shadow-md overflow-hidden h-full flex flex-col">
                                <div class="h-40 bg-gray-300 relative">
                                    {% if book.getImageUrl() %}
                                        <img src="{{ book.getImageUrl() }}" alt="{{ book.title }}" class="w-full h-full object-cover">
                                    {% else %}
                                        <div class="w-full h-full flex items-center justify-center bg-gray-200">
                                            <span class="text-gray-500">Pas d'image</span>
                                        </div>
                                    {% endif %}
                                    {% if book.isForAdult %}
                                        <div class="absolute top-2 right-2 bg-red-500 text-white text-xs px-2 py-1 rounded">18+</div>
                                    {% endif %}
                                </div>
                                <div class="p-4 flex-grow flex flex-col">
                                    <h3 class="font-semibold text-lg mb-1 line-clamp-1">{{ book.title }}</h3>
                                    <p class="text-sm text-gray-500 mb-2">Par {{ book.author }}</p>
                                    <p class="text-sm text-gray-600 mb-3 line-clamp-2">{{ book.abstract|default('Aucun résumé disponible.') }}</p>
                                    <div class="mt-auto flex items-center justify-between">
                                        <span class="text-sm text-gray-500">{{ book.releasedAt|date('d/m/Y') }}</span>
                                        <span class="flex items-center text-sm">
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-red-500 mr-1" viewBox="0 0 20 20" fill="currentColor">
                                                <path fill-rule="evenodd" d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" clip-rule="evenodd" />
                                            </svg>
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% else %}
                    <div class="w-full py-8 text-center text-gray-500">
                        Aucun roman récemment publié.
                    </div>
                {% endif %}
            </div>
        </div>
    </section>

    <!-- Section Derniers Livres Ajoutés -->
    <section class="mb-16">
        <div class="flex justify-between items-center mb-4">
            <h2 class="text-2xl font-semibold">Derniers Livres Ajoutés</h2>
            <div class="flex gap-2">
                <button id="latest-prev" class="bg-gray-200 hover:bg-gray-300 rounded-full p-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                    </svg>
                </button>
                <button id="latest-next" class="bg-gray-200 hover:bg-gray-300 rounded-full p-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                    </svg>
                </button>
            </div>
        </div>

        <div class="relative">
            <div id="latest-carousel" class="carousel-container flex overflow-x-scroll overflow-y-hidden hide-scrollbar gap-4 pb-4">
                {% if booksLatest|length > 0 %}
                    {% for book in booksLatest %}
                        <div class="carousel-item flex-none w-64">
                            <div class="bg-white rounded-lg shadow-md overflow-hidden h-full flex flex-col">
                                <div class="h-40 bg-gray-300 relative">
                                    {% if book.getImageUrl() %}
                                        <img src="{{ book.getImageUrl() }}" alt="{{ book.title }}" class="w-full h-full object-cover">
                                    {% else %}
                                        <div class="w-full h-full flex items-center justify-center bg-gray-200">
                                            <span class="text-gray-500">Pas d'image</span>
                                        </div>
                                    {% endif %}
                                    {% if book.isForAdult %}
                                        <div class="absolute top-2 right-2 bg-red-500 text-white text-xs px-2 py-1 rounded">18+</div>
                                    {% endif %}
                                </div>
                                <div class="p-4 flex-grow flex flex-col">
                                    <h3 class="font-semibold text-lg mb-1 line-clamp-1">{{ book.title }}</h3>
                                    <p class="text-sm text-gray-500 mb-2">Par {{ book.author }}</p>
                                    <p class="text-sm text-gray-600 mb-3 line-clamp-2">{{ book.abstract|default('Aucun résumé disponible.') }}</p>
                                    <div class="mt-auto flex items-center justify-between">
                                        <span class="text-sm text-gray-500">{{ book.createdAt|date('d/m/Y') }}</span>
                                        <span class="flex items-center text-sm">
                                            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-red-500 mr-1" viewBox="0 0 20 20" fill="currentColor">
                                                <path fill-rule="evenodd" d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z" clip-rule="evenodd" />
                                            </svg>
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% else %}
                    <div class="w-full py-8 text-center text-gray-500">
                        Aucun roman récemment ajouté.
                    </div>
                {% endif %}
            </div>
        </div>
    </section>

    <!-- Section Livres les Plus Populaires -->
    <section class="mb-16">
        <div class="flex justify-between items-center mb-4">
            <h2 class="text-2xl font-semibold">Livres les Plus Populaires</h2>
            <div class="flex gap-2">
                <button id="top-prev" class="bg-gray-200 hover:bg-gray-300 rounded-full p-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
                    </svg>
                </button>
                <button id="top-next" class="bg-gray-200 hover:bg-gray-300 rounded-full p-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                    </svg>
                </button>
            </div>
        </div>

        <div class="relative">
            <div id="top-carousel" class="carousel-container flex overflow-x-scroll overflow-y-hidden hide-scrollbar gap-4 pb-4">
                {% if booksTop|length > 0 %}
                    {% for book in booksTop %}
                        <div class="carousel-item flex-none w-64">
                            <div class="bg-white rounded-lg shadow-md overflow-hidden h-full flex flex-col">
                                <div class="h-40 bg-gray-300 relative">
                                    {% if book.getImageUrl() %}
                                        <img src="{{ book.getImageUrl() }}" alt="{{ book.title }}" class="w-full h-full object-cover">
                                    {% else %}
                                        <div class="w-full h-full flex items-center justify-center bg-gray-200">
                                            <span class="text-gray-500">Pas d'image</span>
                                        </div>
                                    {% endif %}
                                    {% if book.isForAdult %}
                                        <div class="absolute top-2 right-2 bg-red-500 text-white text-xs px-2 py-1 rounded">18+</div>
                                    {% endif %}
                                </div>
                                <div class="p-4 flex-grow flex flex-col">
                                    <h3 class="font-semibold text-lg mb-1 line-clamp-1">{{ book.title }}</h3>
                                    <p class="text-sm text-gray-500 mb-2">Par {{ book.author }}</p>
                                    <p class="text-sm text-gray-600 mb-3 line-clamp-2">{{ book.abstract|default('Aucun résumé disponible.') }}</p>
                                    <div class="mt-auto flex items-center justify-between">
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endfor %}
                {% else %}
                    <div class="w-full py-8 text-center text-gray-500">
                        Aucun roman populaire trouvé.
                    </div>
                {% endif %}
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="bg-blue-600 text-white py-16">
        <div class="container mx-auto px-4 text-center">
            <h2 class="text-3xl font-bold mb-4">Prêt à commencer votre voyage littéraire?</h2>
            <p class="text-xl mb-8 max-w-3xl mx-auto">Rejoignez notre communauté de passionnés de littérature dès aujourd'hui.</p>
            {% if app.user %}
            {% else %}
                <div class="flex justify-center space-x-4">
                    <a href="{{ path('app_register') }}" class="px-6 py-3 bg-white text-blue-600 rounded-lg hover:bg-gray-100 transition duration-300">S'inscrire</a>
                    <a href="{{ path('app_login') }}" class="px-6 py-3 bg-transparent border-2 border-white text-white hover:bg-white hover:text-blue-600 rounded-lg transition duration-300">Se connecter</a>
                </div>
            {% endif %}
        </div>
    </section>
</div>

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Function to handle carousel scrolling
            function setupCarousel(carouselId, prevId, nextId) {
                const carousel = document.getElementById(carouselId);
                const prevBtn = document.getElementById(prevId);
                const nextBtn = document.getElementById(nextId);
                
                if (!carousel || !prevBtn || !nextBtn) return;
                
                const scrollAmount = 320; // Approximate width of a card + gap
                
                prevBtn.addEventListener('click', function() {
                    carousel.scrollBy({ left: -scrollAmount, behavior: 'smooth' });
                });
                
                nextBtn.addEventListener('click', function() {
                    carousel.scrollBy({ left: scrollAmount, behavior: 'smooth' });
                });
                
                // Hide scrollbar but keep functionality
                carousel.classList.add('hide-scrollbar');
            }
            
            // Setup all three carousels
            setupCarousel('newest-carousel', 'newest-prev', 'newest-next');
            setupCarousel('latest-carousel', 'latest-prev', 'latest-next');
            setupCarousel('top-carousel', 'top-prev', 'top-next');
            
            // Add CSS to hide scrollbar
            const style = document.createElement('style');
            style.textContent = `
                .hide-scrollbar {
                    -ms-overflow-style: none;  /* IE and Edge */
                    scrollbar-width: none;     /* Firefox */
                }
                .hide-scrollbar::-webkit-scrollbar {
                    display: none;             /* Chrome, Safari, Opera */
                }
            `;
            document.head.appendChild(style);
        });
    </script>
{% endblock %}
{% endblock %}