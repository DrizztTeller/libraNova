vich_uploader:
    db_driver: orm
    mappings:
        novel_files:
            uri_prefix: /uploads/pdf  # Dossier public pour accéder au fichier
            upload_destination: '%kernel.project_dir%/public/uploads/pdf'
            namer: Vich\UploaderBundle\Naming\OrignameNamer # Optionnel : conserve le nom d'origine
    #mappings:
    #    products:
    #        uri_prefix: /images/products
    #        upload_destination: '%kernel.project_dir%/public/images/products'
    #        namer: Vich\UploaderBundle\Naming\SmartUniqueNamer
